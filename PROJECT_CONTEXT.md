# zimage-auto-light 프로젝트 안내서

> 이 문서는 새 대화를 시작할 때 Claude가 **현재 기능·설계 의도·작업 방식**을 빠르게 파악하도록 돕는 안내서다.
> 실제 코드는 함께 올린 `server.py`, `static/app.js`, `index.html`, `README.md`를 기준으로 한다.

---

## 0. 현재 상태 & 다음 할 일 (2026-06-11 세션 — 새 대화는 여기부터)

**🎯 이번 세션 최대 발견 2가지:**
- ✅ **5060 8GB에서 1024 된다!** (OOM 없이 생성·저장 확인 — 진짜 서비스, MEM_MODE=VRAM, 타일링 ON.) **기존 "8GB 1024 부적합" 결론 폐기.** diag로 원인 규명: 디노이징 루프(transformer)=**6.55GB로 8GB에 여유 있게 들어옴**, 넘치던 8.46GB는 **1회성 VAE 디코드** → 타일링이 잡아줌. (옛 "384초/부적합"은 타일링 끈 benchmark값이었음.)
- 🔑 **Triton이 줄곧 깨져 있었다(eager 폴백).** 로그 "Failed to find C compiler" — 이미지에 C 컴파일러가 없어 SDNQ uint4 최적커널 미사용. **5060·5090·A100 모든 속도 측정이 eager = 전부 과소평가됐을 수 있음.** → Dockerfile에 `build-essential`+`python3-dev` 추가(v8). **빌드 후 옛 속도값 전부 재측정 대상.**
- 5060 1024·8step 실측(eager) = **~24초/장**(step당 ~3초, OOM 없음). gcc 살린 v8에서 재측정 필요.

**이번 세션 한 일(코드):**
- `diag_vram.py`(단계별 VRAM 프로파일러, v7 포함). ⚠️ 한계: forward 훅이라 `vae.decode()`는 못 잡음 → 판정이 "transformer가 peak"로 **오표기**됨(실제 전체 peak는 VAE 디코드).
- **활용 시나리오 탭**(v8 UI) — 비교 뷰 중첩 서브탭: `GPU 목록`(기존 '카탈로그' 개명)/`비교 분석`/`활용 시나리오`(동시·대량·구성). 클래스 **`usc-` 격리**. 구성 유연성에 **목표 장수 +/−** 추가(목표 완료시간·총비용으로 비교 → "여러 대 = 더 빨리·여전히 싸게"). "비싼 메인+싼 백업" 설명 카드. 숫자 하드코딩(5.8 기반). 검증 `node --check` OK.
- **server.py: MEM_MODE 기본 VRAM**(기존 RAM)으로 변경 — env 없어도 VRAM. RAM은 폴백.
- **Dockerfile: build-essential+python3-dev**(Triton 살리기) + `COPY diag_vram.py`.
- 참고 리서치: 이미지 생성 **체감 속도** — UX 한계선 ~10초, 쾌적 ~5초, "느림" 1분+. (5060의 24초는 아직 한계선 초과 → Triton·step으로 단축 필요.)

**확정 결과(06-11, v8/v9 배포):**
- ✅ **Triton 작동**(gcc 후 fail 사라짐). 5090 워밍업 후 **~2초/장**(eager 3.09 대비 ~15-20%↓, 첫 장은 컴파일로 4초). **5060은 ~24초/장 그대로**(Triton 효과 거의 없음 — 약한 카드라 raw 연산이 병목).
- ✅ **1024+8GB 됨**(OOM 없이 생성, 타일링). "8GB 부적합" 폐기. **단 5060 24초 = 빠르지 않음** → 5060은 **"빠른 단일"❌ / "싸고 느린 대량·분산 N대"✅**(원/장 싸면). 5060 8GB throughput ≈ **150장/h**(가격 미측정).
- ✅ **v9 리팩토링 라이브**(`/api/resources` 폴링 제거 확인, 4→3).

**🔑 측정 원칙 — 무조건 같은 조건이어야 비교 의미 있음.** 옛 v6 측정값(H100 2.07·5090 3.09·A100 등, "5090 16% 저렴")은 **eager + 타일링 불일치** → **잠정(지우지 말고 유지) → 동일조건 재측정 끝나면 교체.** 재측정 = **v9 이미지(gcc + benchmark.py 타일링 통일) + 전 카드 `--batch 1` 동일조건**(5060은 batch1만 가능 = 공통분모). benchmark.py 이제 server와 동일 로드(타일링 항상 ON)라 **5060도 benchmark로 측정 가능**.

**🔬 v10 재측정 진행표 (동일조건: batch1 · Triton ON · 타일링 ON · VRAM · 1024 · 8step):**
| 카드 | 장당 | 장/h | util | 시간당 가격 | 원/장 | 상태 |
|---|---|---|---|---|---|---|
| H100 80GB (타사 RunPod) | 1.668s | 2,158 | 96% | $3.30(Secure)→4,950원/hr | **2.29원** | ✅ A100도 Secure($1.49)=같은티어 일관 |
| H100 80GB (gcube) | 1.68s | 2,142 | 96% | 6,440~9,200원/hr(TIER1, util96%≈9,080) | **~4.2원** | ⚠️RunPod H100(2.29)보다 ~1.9배 비쌈 — gcube 기본금(6,440/hr). 같은칩인데 타사가 쌈 |
| **RTX 5090 (gcube)** | **2.97s** | **1,210** | 99% | **2,224원/hr**(실측 320장) | **1.84원**(실측·batch1) | ✅확정 (batch8 sweet spot=1.63) |
| A100-40GB (gcube) | 3.87s | 930 | 98% | ~4,273원/hr | ~4.6원 | 대형·고메모리 작업용(홍보=작업맞춤 선택지) |
| A100-80GB (타사 RunPod) | 3.516s | 1,024 | 98.7% | $1.49≈2,235원/hr | **2.18원** | ⚠️gcube A100-40GB(4.6원)의 절반! gcube 기본금 탓. (변형도 40↔80GB 다름) |
| RTX 4090 (gcube) | 4.562s | 789 | 99.6% | all-in~2,937/hr(짧은측정) | ~3.7원(잠정) | 예상보다 느림(보통 4090>A100) → gcube 인스턴스 제약 가능성, 긴 런으로 재측정 권장 |
| RTX 5060 8GB (gcube) | **~24s**(비혼잡) | **~150** | 99% | (측정) | — | 비혼잡 기준. ⚠️Tier3 공유라 혼잡 시 2~3배↑(49~74s 관측) → 안정 필요하면 전용티어. (v8 서버 측정, 오프피크 v10 재확인 권장) |
> H100 원/장 = 5,033 ÷ 2,158 ≈ 2.33(batch1 기준 — 옛 1.94는 H100 batch8 sweet spot값이라 5060과 동일조건 아님). 옛 eager H100 batch1=2.07s → Triton 1.67s(~19%↑). **Triton은 강카드만 효과(5060엔 무효).** gcube 3개 throughput+가격 채워지면 → 옛 값 교체 + 활용시나리오 탭 실측 반영.

**✅ 완료(2026-06-12~13):** v10 동일조건 측정(H100·5090·A100·4090·5060) + 타사(RunPod H100/A100) 비교 + **활용시나리오 탭 v10 실측 전면 교체**(index.html·static/app.js·_preview, 동시·대량·구성 숫자+%). 측정 상세 = `측정기록_v10_20260612.md`.

**🎯 최종 결론:** ① **gcube 5090 = 1.84원/장 = 전체 최저원가**(타사 H100 2.29·A100-80GB 2.18보다 쌈) → "가벼운 이미지 생성은 gcube 5090이 최저원가". ② **같은 칩이면 타사가 쌈**(gcube H100 ~4.2 vs 타사 2.29) — gcube 데이터센터 카드는 기본금 탓에 비쌈. → **홍보: "gcube 5090(소비자카드)이 무기"**, "gcube가 다 싸다"는 금지(거짓).

**다음 할 일:**
1. **git 커밋·push** — 이번 세션 변경 다수(측정기록 md·PROJECT_CONTEXT·index.html·app.js·benchmark.py·diag·Dockerfile). `git add -A && git commit -m "..." && git push`.
2. **v11 빌드·재배포** — 탭 교체가 UI라 새 이미지 빌드해야 반영(`docker build -t yjoh/z-image-light:v11 .` → push → 배포).
3. **5060 8GB 가격** (오프피크 비혼잡 클린 측정) → 탭 5060 슬롯 채움.
4. (선택) 4090 재측정(throttle 확정) / gcube H100 정밀청구.

**주의(gotchas):**
- **비교는 무조건 같은 조건**(같은 이미지·Triton ON·타일링 ON·VRAM·1024·8step·**batch1 통일**). **옛 속도/원장 = 잠정 — 확정 보고 금지.** 재측정 후 교체.
- **Triton: C컴파일러(build-essential, v8+) 필요.** 없으면 eager(느림). 단 5060은 Triton 켜도 ~24초(카드 한계).
- **1024+8GB는 됨**(타일링 ON·MEM_MODE=VRAM·server.py). 넘치던 건 1회성 VAE 디코드. 아주 빠듯하면 `MEM_MODE=RAM` 폴백(느림).
- **diag/벤치는 workload command 칸 단독 실행**(`...; sleep infinity`, 서버랑 같이 X — 모델 2개 OOM). URL 접속 에러는 정상. **diag는 MEM_MODE env에 휘둘림**(VRAM 세팅이면 RAM 측정 안 됨).
- **활용 시나리오 = v8 UI**, 하드코딩(동시 40%·대량 16%·5090 1.63·H100 1.94), `usc-` 격리, JS는 `showScn`/`concCalc`/`bulkCalc`/`flexCalc`(app.js).
- **깨끗한 비용 = 고정장수 연속·한 UTC시·idle0 → 청구÷장수.** gcube=기본금+이용금. **환율 1,525원/$.** Tier2 안정/Tier3 공유(14배 변동).

---

## 1. 프로젝트가 무엇인가

gcube GPU 클라우드에 **Z-Image-Turbo(uint4 양자화) 분산 이미지 생성 서비스**를 배포한다.
- **REST API + 모니터링 웹 UI** 한 컨테이너.
- 여러 노드(레플리카)에 같은 이미지를 띄워 **분산 생성**, 결과·상태를 웹 대시보드로 모니터링.
- repo: `github.com/chaeyoon-08/zimage-auto-light`
- 이미지: `ghcr.io/chaeyoon-08/zimage-auto-light-uint4:latest` (~14GB) / Docker Hub `chaeyoon08/zimage-auto-light-uint4` (수동 push)

### 파일 구성
- `server.py` — FastAPI 서버(생성 엔진 + API + 상태 집계)
- `index.html` — 웹 UI(생성 / 대시보드 / 이미지 / **비교** / 사용법 탭)
- `static/app.js` — UI 로직 / `static/style.css` — 스타일 / `static/logo.png`
- `Dockerfile`, `entrypoint.sh`, `build.yml`(GitHub Actions → ghcr), `conditions.json`
- `gpu_profiles.json` — 비교 탭용 GPU 가격·과금정책(가격/정책/정체성만, 성능 수치 X). conditions.json과 별개
- 검증 하네스: `/tmp/verify_server.py`(server, mock), `/tmp/verify_index.js`(jsdom)

---

## 2. 핵심 설계 (왜 이렇게 되어 있나)

### 레플리카 구조
- 각 레플리카(파드)는 **독립적으로** GPU·모델·생성 엔진을 가짐. 결과는 **공유 폴더**에 씀.
- 읽기는 공유 폴더라 일관되지만, 쓰기(생성)는 응답한 그 레플리카 하나만 한다.

### 폴더 구조 (RUN_ID 격리)
- `deployment` = 파드 이름에서 자동 추출(`rsplit("-",2)[0]`, 예: `dep-000-xxxx-yyyy` → `dep-000`)
- `RUN_ID` = 작업 구분 키(환경변수, **권장**이지만 필수 아님. 미지정 시 `default`)
- 폴더: `/workspace/current/<deployment>/<RUN_ID>/`  (메타 json·status·history)
- 원본 PNG는 `/workspace/outputs/{auto|manual}` 에 한 번만 저장(용량 중복 방지), 메타 json만 위 폴더에.
- **조회 범위(scope):** `run`=이번 RUN_ID 폴더 / `all`=그 deployment 밑 모든 RUN_ID 폴더
- 같은 RUN_ID로 재배포하면 이어쓰기(오류 없음), 다른 값이면 폴더가 갈려 안 섞임.

### 메모리 모드 (MEM_MODE) — 노드 사양에 맞춰 선택
- 값은 **`VRAM` / `RAM`** 둘만 (기본 `RAM`, 잘못된 값은 `RAM`으로, 대소문자 무관)
- `VRAM` = 모델을 VRAM에 올림(`pipe.to("cuda")`) → 생성 중 RAM 거의 안 씀
- `RAM` = 모델을 RAM에 둠(`enable_model_cpu_offload`) → VRAM은 컴포넌트씩만

| 노드 사양 | MEM_MODE | 이유 |
|---|---|---|
| VRAM 8GB · RAM 24GB | `RAM` | VRAM 빠듯, RAM 여유 |
| VRAM 12GB · RAM 16GB | `VRAM` | VRAM 여유 |
| VRAM 16GB · RAM 16GB | `VRAM` | VRAM 넉넉 → 속도 이득 |
| VRAM 8GB · RAM 16GB | (지원 제외) | 양쪽 빠듯. 정 필요하면 `RAM`+해상도 768 → sequential → 더 공격적 양자화 순 |

- 참고: uint4 모델 VRAM 점유 ~5GB(활성화·VAE 합쳐 ~10GB), `RAM` 모드 실질 최소 RAM은 24GB.
- RAM은 시스템·라이브러리 기본 점유가 이미 있어 같은 16GB여도 VRAM보다 불안. RAM OOM은 프로세스를 통째로 죽임.

### 죽은 레플리카 처리
- 갱신이 오래(기본 5분) 끊긴 레플리카는 **집계 단계에서 제외** → UI에 안 나옴(옛 작업 잔존 status 포함).
- "죽음"은 영구가 아님 — 매 집계(2초)마다 age로 재판정. 갱신 재개하면 자동 복귀.
- 부하는 Refresh-Ahead로 낮음(백그라운드 스레드가 2초마다 한 번 집계, 화면은 메모리만 읽음).

### 환경변수 5개
`MEM_MODE` / `RUN_ID` / `GEN_COUNT`(자동 생성 장수) / `CONDITIONS_FILE`(조건 파일 경로) / `RANDOM_PICK`
- 환경변수를 **하나도 안 넣어도 배포됨**: `MEM_MODE=RAM` + `RUN_ID=default` + 자동생성 없음(UI 수동만).
- `GEN_COUNT`와 `CONDITIONS_FILE` 둘 다 채워야 배포 시 자동 생성이 돈다.
- `conditions.json`은 이미지에 COPY되지 않음 → 자동 생성 쓰려면 마운트 저장소에 둬야 함.

---

## 3. 작업 방식 (반드시 지킬 것)

- **역할 분담:** 대화(Claude)는 설계·검증·파일 작성 담당. 사용자는 다운로드·git 커밋·재배포 담당.
  - 파일은 대화가 직접 작성 → `present_files`로 공유 → 사용자가 다운로드해 repo에 덮어씀.
  - Claude Code에 파일 작성/수정을 맡기지 않는다(창의적 판단으로 일을 그르침).
- **추측 금지:** 코드를 직접 보고 줄 번호 근거로 진단한다. 모르면 검색하거나 사용자에게 확인.
- **검증 필수:** 수정 후 `verify_server.py`(server)·`verify_index.js`(jsdom) 통과 확인.
- **진행 방식:** 브리핑 → 컨펌 → 실행. 큰 변경을 한 번에 몰지 않는다. 시킨 것만 정확히 하고, 임의로 다른 것을 만들지 않는다.
- **빌드 비용:** GitHub Actions 빌드는 시간이 걸리니, 여러 변경은 모아서 한 번에 반영.
- **적용 안내 표현:** "네가 다운로드해서 덮어써야 함"처럼 주체를 명시. 명령조 금지.
- **기록·기억 방식(중요):** 머릿속으로 다 외우지 않는다. 결정·설계를 이 문서(PROJECT_CONTEXT.md)와 메모리에 남기고, 새 대화는 이 문서를 먼저 읽혀 이어간다. 긴 회의 끝엔 "여기까지 정리해서 기록해줘"로 이 문서를 갱신한다. (자세한 사용법은 인수인계 가이드 참고.)

### gcube 배포 관련
- 워크로드 yml은 **배포 설정**이라 repo에 커밋하지 않음(참고용). 사양별 템플릿은 `deploy/`에 둠.
- GPU 코드는 추측 금지 — `gcube gpu list`로 확인해서 채운다.
- 마운트: 클라우드 저장소를 `/workspace`에 연결.

---

## 4. 변경 이력 (최근)

### 2026-06-09 (버그수정 + B-1/B-2 설계 논의)
- **대시보드 깜빡임 수정:** `app.js`의 `mergeReps`가 응답 1번 누락에도 레플리카를 즉시 삭제 → 사라졌다 나타나는 깜빡임. **REP_GRACE_MS(12초) 유예** 추가(일시 누락 무시, 오래 사라진 것만 제거).
- **생성량 재시작 불일치 수정:** `generated`가 프로세스 누적 카운터(`MY_GENERATED`)라 파드 재시작 시 0으로 리셋 → 디스크 이미지와 안 맞음. 부팅 시 `CURRENT_DIR`에서 `{REPLICA_ID}_*.json` 수를 세어 `BASE_GENERATED`로 복원, `generated=BASE+세션`. throughput은 세션 카운터(`session_generated`) 유지로 인플레 방지.
- **Dockerfile 빌드 수정:** `hf download`(새 CLI, PATH에 없어 exit 127) → `python3 -c snapshot_download`로 교체(demo와 동일, 버전·PATH 영향 없음).
- **레플리카 상세 모달에 "동일 스펙 타 서비스 시간당 단가" 표 이식**(demo→현재, 정적 HTML+`.cmp` CSS).
- **버그(JSON 생성이 AUTO):** 현재 코드엔 없음(이미 `f59fc98`에서 수정). 증상은 **배포 이미지가 수정 전 버전**이라서 → 재빌드/재배포로 해결. (gcube는 `yjoh/z-image-light` 레지스트리 이미지를 받으므로, 새 태그로 push + 재배포 필요)
- **크래시루프(CrashLoopBackOff) 원인=CRLF:** 현재 프로젝트에 `.gitattributes`가 없어 Windows에서 `entrypoint.sh`가 **CRLF**로 저장됨 → 컨테이너 셔뱅 `#!/usr/bin/env bash` 깨져 exit 127 → 무한 재시작. **`.gitattributes` 추가(`*.sh eol=lf`) + entrypoint.sh를 LF로 변환**해 해결(demo 폴더엔 원래 있던 보호장치). 빌드는 되는데 실행만 죽으면 이거부터 의심.

- **B-1 메모리 절약옵션 — 끄기 시도했다가 되돌림(회귀):** v5에서 VRAM 모드일 때 `vae_slicing/vae_tiling/attention_slicing`을 껐는데, **벤치 결과 5060Ti(16GB)에서 장당 21s→116s(약 5배 느림)**. 약한 카드엔 이 옵션들이 속도에도 도움 → **항상 켜는 원래 동작으로 원복**(server.py). 교훈: "VRAM 모드=여유 카드"가 아님(5060Ti도 VRAM 모드 씀). 강한 카드(H100 등)에서 끄는 건 별도 검증 후. **벤치마크(benchmark.py)가 이 회귀를 즉시 잡아냄.**
- **벤치마크 도구 추가(benchmark.py):** server와 동일 로드로 GPU별 성능 측정(장당시간·throughput·VRAM peak·util, 배치 sweep). Dockerfile에 COPY. 어느 클라우드든 `docker run ... python3 /app/benchmark.py --sweep`로 공정 비교. gcube는 command 오버라이드한 일회성 워크로드로.
- **A-3 전체 레플리카 일괄 생성 (Phase 1):** control 프로토콜 확장. `_check_control`가 타겟(`control/<pod>.json`) + **브로드캐스트(`control/_broadcast.json`)** 둘 다 읽고, `generate` 액션 추가(각 pod가 자기 사이클에 읽어 **각자 count장** 생성, 진행 중이면 건너뜀, ts≥STARTED_AT 보장). 새 API `POST /api/broadcast`(생성 폼과 동일 입력). UI: 생성 탭 **"전체 레플리카에 각자 Count장" 토글**(`bcastAll`) → 켜면 `/api/broadcast`로 fire-and-forget. 의미=각자 N장(합산 아님). **미구현(Phase 2 후보):** 선택 레플리카 지정 생성, 완료 추적(현재는 대시보드/갤러리로 확인), 새로 합류한 pod는 옛 브로드캐스트 미실행.

### 2026-06-08 (비교 탭 v1)
- **비교 탭 신설(4번째 위치):** 생성·대시보드·이미지·**비교**·사용법. GPU 종류 단위로 성능·비용(원/장) 비교.
  - 카탈로그(자사/경쟁사 GPU 카드) + 비교 분석(컬럼 비교, 조건 종속 드롭다운, 실측/예측, 원/장 막대그래프, 결과 저장 CSV/JSON).
  - 상세 모달: 비용 도넛 분해 + 조건 선택 + 실측/예측 + 장수/시간 입력.
- **가격·정책 로드:** `gpu_profiles.json`에서 카드 동적 생성. `GET /api/gpu_profiles`(읽기) / `POST /api/gpu_profiles`(UI 업로드 저장 → `/workspace/gpu_profiles.json`). 프로파일 불러오기/내보내기/새로고침.
- **사용법 docs**에 비교 탭 + gpu_profiles.json 설정/백업 섹션 추가.
- **범위 한정(중요):** 이번엔 프런트 + 가격 프로파일 백엔드까지만. **성능 데이터(통계함)는 아직 미구현 — 비교 탭의 성능 수치는 placeholder(가데이터 CMP_STATS)**. 통계함 백엔드가 다음 단계.


### 2026-06-05 (push 완료)
- **RUN_ID 구조:** 권장(미지정 시 `default`) + deployment 자동 추출 + 폴더 계층 `current/<deployment>/<RUN_ID>` + 조회 scope(run/all). 갤러리 "이번 작업/전체" 토글은 **이미지 탭에만**(생성 탭 아님).
- **config 생성 버그 수정:** UI에서 config 파일로 생성해도 `manual`로 뜨게(이전엔 auto로 빠짐). source를 출처 기준으로 판정.
- **죽은 레플리카 완전 제거:** 서버 집계에서 제외 + 클라이언트 자체 죽음 재판정 로직 삭제(서버 판정만 신뢰). DEAD 칩·"죽은 것 포함" 토글·범례·docs 정리.
- **MEM_MODE 직관화:** `none/model/sequential` → **`VRAM`/`RAM`** 만(기본 RAM). sequential 제거.
- README·UI 도움말을 위 변경에 맞게 갱신.

### 알아둘 점
- GitHub Actions 빌드에서 uint4 `hf download`가 가끔 exit 1로 실패하나, 재push하면 통과(일시 네트워크/hf_xet 문제). 자주 반복되면 Dockerfile에 `HF_HUB_DISABLE_XET=1` 추가로 영구 해결 가능.

---

## 5. 비교 탭 — 설계 전모 (구현·미구현 명시)

> 비교 탭은 긴 설계 회의를 거쳐 만들었다. 아래는 **왜 이렇게 했는지**까지 담은 전모다. v1에서 구현된 것과 아직 아닌 것을 구분해 둔다.

### 5.1 비용 모델 (핵심)
- 과금은 **"사용량"이 아니라 "대여 시간"** 기준. 플랫폼마다 **시간당/분당** 단위가 다르고 **올림** 적용(시간당은 5분 써도 1시간 청구 → 목표 장수가 작을수록 차이 큼).
- `총비용 = 시간당단가 × 과금시간(올림) + 기본금 + 스토리지`, `원/장 = 총비용 ÷ 장수`. 기본금 = CPU·메모리 등 하드웨어 묶음, 스토리지 = 저장소 대여료.
- **이미지 생성 시간은 GPU 모델이 같으면 어느 플랫폼이든 동일** → 우리가 한 번 측정한 초/장에 각 플랫폼 과금 정책만 대입. **동일 모델 경쟁사는 직접 안 돌려봐도 됨.**
- **실측 / 예측 토글:** 실측=목표 입력 비활성, 통계함 실제 누적값 표시 / 예측=N장·N시간 입력해 가정 계산. 기본 = 전체 조건 + 실측.
- 통화 = **원화 표시**(환율 API 안 씀, GPU 부하). 경쟁사 달러가는 입력 시점 원화 환산, 측정 기준일(KST) 표기.
- 비교 단위 = **GPU 모델 + MEM_MODE(VRAM/RAM)**, 모드 항상 라벨. VRAM/RAM 속도차는 같은 GPU의 다른 운영설정이라 정상(혼동 방지용 라벨).

### 5.2 데이터 출처 — 두 갈래를 GPU 모델명으로 조인
- **가격 프로파일** `gpu_profiles.json`(가격/과금정책/정체성) + **통계함**(측정 성능) → **GPU 모델명(+모드)으로 조인** → 카드.
- **성능 수치는 JSON에 안 적는다**(통계함에서 옴). `gpus[].model`은 통계함 GPU명과 정확히 일치해야 함(유일한 필수 규칙).
- `gpu_profiles.json` 스키마: `{schema_version, currency:"KRW", providers:[{provider, kind:"self|competitor", measured_at(경쟁사만), billing:{unit:"minute|hour", base_fee, storage_fee}, gpus:[{model, mem_mode, price_per_hour}]}]}`. 동봉 `gpu_profiles.json`(가데이터) 참고.

### 5.3 화면
- 카탈로그: `자사(GCUBE)`/`경쟁사` 두 그룹(배지에 회사 실명, 자사=보라 채움·경쟁사=보라 외곽선). 비교 모드로 **2~4개** 선택→분석. 카드 클릭=상세 모달.
- 비교 분석: 컬럼 비교, `예측`/`실측` 구역 헤더 분리, 조건 **해상도→step→guidance 종속 드롭다운 + 조회**, 원/장 막대그래프(최저=초록, 나머지=보라), 초기화, 결과 저장(CSV/JSON 드롭다운). 조건은 실제 생성 파라미터에서 프롬프트·시드 제외.
- 모달: 비용 도넛 분해(GPU시간/기본금/스토리지) + 성능 스탯 + 조건 선택 + 실측/예측 + 장수/시간 입력 + 완료 실행 로그(자사)/측정 정보(경쟁사).

### 5.4 ✅ v1 구현 완료 (2026-06-08)
- 비교 탭 UI 전체(카탈로그·비교 분석·모달), 위 화면/비용모델/조건 로직.
- `gpu_profiles.json` **읽기/업로드 저장**(`GET/POST /api/gpu_profiles`) + 불러오기/내보내기/새로고침.
- 코드 위치: `index.html`(탭·뷰·모달·docs), `static/app.js`(`cmp*`/`CMP_*` 모듈, `show('compare')`), `static/style.css`(`.cmp-*`), `server.py`(`GPU_PROFILES_PATH`, 두 엔드포인트).

### 5.5 ⏳ 아직 안 함 (다음 단계)
1. **통계함(측정 성능) 백엔드** — 현재 비교 탭 성능은 placeholder(`CMP_STATS` 가데이터). 진짜로 만들려면:
   - 위치 `/workspace/stats/`, **RUN_ID·deployment·pod 무관 고정**. 각 pod가 자기 조각 `stats/<pod>.jsonl`에만 append(충돌 없음), 읽을 때 합산(=GPU별 평균 + 완료 실행 목록). history의 recent+rollup 패턴 미러링.
   - 완료 실행 한 줄(잡 단위, 합산 가능하게 count+총합): 신원(gpu_name, mem_mode, pod_id, model_dtype) / 조건(width,height,steps,guidance) / 성능(count, elapsed_total_s, gen_time_min/max, vram_peak_gb) / error_count / started_at·finished_at(KST). **평균은 줄에서 재계산되는 파생물** → 복구 시 보존 핵심은 줄들.
   - 연결점: `server.py`의 `_gpu_snapshot()`에 `nvmlDeviceGetName()` 추가로 GPU 모델명, `_status_payload()`가 이미 평균/throughput/vram peak 계산 → 적립 재료.
   - 비교용 집계 API + `app.js`의 `CMP_STATS`를 fetch로 교체.
2. **통계함 백업/복구 + UI 업로드(통계)** — 통계함이 사용자 고유 데이터(우리가 못 줌, 마운트 날아가면 소실)라 **진짜 백업 대상**. 복구 = **병합**(GPU+조건 키 누적 합산, 중복 줄은 완료시각+pod로 제거). 업로드는 가격 프로파일과 동일 방식(아무 pod나 받아 공유 마운트에 씀)으로 `/workspace/stats/`에. 내보내기/백업 3종(비교결과=읽기용 / 가격프로파일 / 통계백업)은 성격이 달라 섞지 말 것.
3. **레플리카 상태 판정 정교화** — 현재 heartbeat age 단일(DEAD_SECONDS)을 **busy/idle/dead** 셋으로. busy=살아있고 GPU사용, idle=살아있고 nvidia-smi 응답하나 GPU 미사용(완료/대기, 죽은 게 아님), dead=nvidia-smi 연결 끊김.
4. **제어 메커니즘 확장** — 공유 폴더 control 패턴 확장(기술 가능 확인됨). 브로드캐스트(`control/_broadcast.json` 전 pod 읽음 → "모든 레플리카 각자 N장 생성") / 지정(`control/<pod>.json`) / 그룹(GPU·모드별). 셋 다 "공유 폴더에 명령 남기고 pod가 읽어 실행" 한 메커니즘. (미정: 브로드캐스트가 "각자 N장" vs "합쳐 N장" — 각자 N장 추천.)
5. **시간 추이 그래프** — history에 누적 생성량 적립 후 2단계.

### 5.6 새로 추가된 요청사항
- 별도 문서 **`앞으로_할_일.md`** 참고(이 문서와 함께 전달됨). 아직 구체화 전 항목들.

### 5.7 B-1·B-2 설계 논의 (2026-06-09)
> 경영진 요청 B-1(노드 최대 활용)·B-2(성능·경제성 우위 어필)을 논의해 방향을 잡았다. 아직 미구현, 회의 기록.

**전체 로드맵(의존 순서):**
> **B-1**(GPU 풀로 돌리게 수정) → **통계함**(측정 인프라, 5.5-1) → **B-2**(구성·시나리오 비교 + 타사 모델 데이터) → **B-3**(프레임을 토큰까지 일반화)

**B-1 — "최대 활용"의 정체 = 코드 작업(배포 노브 아님).**
- 현재 이미지는 VRAM을 **최소화**하게 설정돼 있음(8GB 카드 기준): 기본 `MEM_MODE=RAM`→`enable_model_cpu_offload`(컴포넌트씩만 VRAM), 그리고 `enable_vae_slicing/vae_tiling/attention_slicing`(server.py 150~159) 전부 켜짐. 그래서 "VRAM 남음"은 부분적으로 의도된 결과.
- `GEN_COUNT`(개수) 늘리기 = 1장 생성을 순차 반복일 뿐 → GPU 순간 부하 그대로. **최대 활용 아님.**
- VRAM 채우는 레버(영향순): ① `MEM_MODE=VRAM`(env, 즉시·코드X) ② 해상도↑(env, 산출물 바뀜) ③ 절약옵션 끄기(작은 코드, 큰 카드 전용) ④ **배치(`num_images_per_prompt=N`, server.py:242 — 핵심 코드변경, VRAM을 N배로)**.
- 비용 핵심: GPU는 **시간당 과금** → 다 쓰든 적게 쓰든 같은 요금 → 빈 VRAM은 절약이 아니라 **버린 비용**(전세버스 빈자리). 큰 카드는 채워야 장당 싸짐.
- ⚠️ 배치/절약옵션 끄기는 빠듯한 카드(8GB) OOM 위험 → MEM_MODE 따라 조건화 필요. 양자화(SDNQ uint4)+offload 경로에서 배치 지원되는지 **작은 테스트로 먼저 검증**(B-1 0단계).

**B-2 — 메시지를 "싸다" → "사업자에게 더 경제적이다"로. 이기는 축 3개:**
| 경영진 예시 | 이기는 축 | 보여줄 것 | 비교 탭 확장 |
|---|---|---|---|
| 예시1 묶음(A100 1개 vs 5060 3개) | ① 총 처리량 | 목표 N장 끝내는 총시간·총비용(합산 throughput) | GPU 1개→**"구성(여러 개 묶음)" 단위** 비교 |
| 예시2 동시처리(H100 순차 vs 5060 5대) | ② 동시성(가장 강함) | 동시 M명 처리 슬롯·1인당 대기(GPU 수=병렬 슬롯) | **"동시 N명" 시나리오** 입력 |
| 예시3 백업 구성 유연성 | ③ 구성 유연성 | 같은 목적을 더 싼 GPU 조합으로 | **GPU 자유 조합 구성기** |
- 표현 주의: 단일 1장 속도는 H100/A100이 빠름. 우리가 이기는 건 **"같은 돈·시간에 더 많이/여러 명"**(총처리량·동시성)이지 "한 장 빠름"이 아님.
- 비교 단위 확장: **GPU 1개 원/장 → 구성(여러 GPU 조합) + 시나리오(목표장수·동시사용자·예산).**

**공통 전제(미해결 과제):**
- B-1 실측이 없으면 묶음·동시 숫자가 가짜 → 통계함 + B-1 선행 필수.
- **타사 모델(H100/A100) 성능 데이터를 어떻게 넣을지** 미정 — 현재 비교 탭은 "같은 모델" 가정(5.1)이라 다른 모델 비교 설계가 비어 있음. 옵션: 공개 벤치/추정 vs 한 번 빌려 실측.

**B-3 (LLM 토큰 비교) — 보류, 방향만 메모 (2026-06-09):**
- 지금 안 함. 이미지(B-1·B-2) 먼저. 아래는 합의된 접근 방향.
- 비교 상대 = **API 사용량 과금(OpenAI 등) vs 자체 호스팅(gcube 시간 대여)**. (다른 GPU클라우드 비교는 이미지 비교의 토큰 버전일 뿐.)
- 단위 = **원/1M토큰** 통일. gcube = 시간당단가 ÷ 토큰 throughput(측정 필요 = LLM판 B-1).
- 핵심 화면 = **사용량–비용 손익분기 그래프**: API=원점서 직선(쓴 만큼), gcube=월 고정 평평선 → 교차점. 소량/간헐=API 승, 대량/꾸준=gcube 승.
- 정직성: 소량이면 API가 쌈 + API는 운영부담 0(자체호스팅은 손이 감)도 명시.
- 전제: vLLM 등 **LLM 서빙 컨테이너 신규 제작** + 토큰 throughput 측정 → 별도 프로젝트.

### 5.8 벤치마크 측정 결과 (2026-06-10, 진행 중)
> `benchmark.py`로 GPU별 성능 측정 = 비교(B-2)의 "성능" 재료. 조건 동일: uint4 / MEM_MODE=VRAM / 1024×1024 8step.

**✅ H100 80GB HBM3 (RunPod, Secure cloud=전용) — 측정 완료**
- 가격: Compute **$3.29/hr** + 컨테이너스토리지 $0.003 + 볼륨 $0.003 = **합계 $3.30/hr** (≈4,455원 @1,350원/$). 정지 시 idle 디스크 $0.006/hr.
- 환경: H100 SXM 1장, vCPU 28, RAM 251GB, CUDA 13.0, 이미지 `yjoh/z-image-light:v6`.
- 성능: batch1 **2.072s·1738장/h**(VRAM 8.48GB,util98%) / batch8 **1.388s·2595장/h**(VRAM 22GB, sweet spot) / batch32 1.334s·2698장/h(VRAM 54GB, 포화).
- **원/장**: batch1 ~2.56원 / batch8 ~1.72원 / batch32 ~1.65원.

**✅ RTX 5090 32GB (gcube Tier2) — 측정 완료, Tier2는 안정적!**
- 성능: batch1 **3.092s·1164장/h**(util99%) / batch8 **2.905s·1239장/h**(VRAM22GB) / batch16 OOM(32GB 한계).
- **배치 이득 거의 없음(~6%)** — batch1부터 util 99~100% = 완전 compute-bound → 5090엔 배치 무의미.
- **Tier2라 런마다 동일(안정적)** — Tier3 5060 14배 변동과 대조. → **벤치는 Tier2에서.**
- 가격(청구구조): gcube = **기본금액(910원/hr, 시간 기반) + 이용금액(GPU 사용량 기반)**. 짧은 벤치 청구는 노이즈 큼(준비시간·시간대쪼개짐·idle로 36~265원 들쭉날쭉) → 신뢰 X.
- **✅ 5090 원/장 = 1.63원 (클린 측정 확정!):** 320장 연속생성(batch8×40, util 97%, idle 없음) → 청구 **520.66원 ÷ 320장 = 1.63원/장**(기본 0.68 + 이용 0.95). 생성 중 all-in ≈ **2,172원/hr**(기본910+이용1,262). ※ 앞선 1.24(idle 저평가)·5원(짧은런 과장)은 폐기 — **1.63이 진짜**.
- **🎯 5090 vs H100 (확정):** 원/장 **5090 1.63 vs H100 1.94(batch8) → ~16% 저렴.** (batch1/동시성 기준은 H100 비효율이라 ~36%.) **gcube가 싼 건 맞지만 margin은 모던(~16%).** 앞서 36~48%는 잘못된 1.24 기준 과장 → 목업·문서 1.63으로 갱신 필요.
- **교훈:** gcube 비교는 반드시 **all-in(기본+이용+스토리지)** 으로. 기본금만 쓰면 과장됨. (정직성·경영진 신뢰 직결)
- **비교 대상 확정 = RunPod H100 SXM $3.29/hr** (웹조사: RunPod 정가, 당신 실측과 일치). 타 공급사(Lambda $4.29 / Vast PCIe ~$2 / spot $1대)는 참고만. **단일 원/장은 박빙**(gcube all-in 1.24 vs H100 1.72, 싼 H100이면 거의 동률) → 자랑 말고 시나리오로.
- **유스케이스(동시성) 탭 방향:** "자랑판" 아니라 **의사결정 도구**. 시나리오 ①동시 사용자(N명 → H100 1대 vs 5090 N대: 동시슬롯·대기·비용, 데이터 적게 듦·제일 강함) ②대량생성/묶음(목표장수 → 5090 N대 vs H100 1대: 완료시간·총비용, **실측데이터 있음** — 3×5090=3,717장/h·2,730원 vs H100 2,595장/h·4,455원) ③예산고정(월예산 → 산출량). 정직성: 단일 1장 속도는 H100 우위 병기.

**⏳ RTX 5060Ti 16GB (gcube Tier3) — 재측정 필요(Tier3 공유라 변동), 가격 미확보**
- best-case(한가할 때, v4 수동 4장): **~23.5초/장 (~153장/h)**.
- 혼잡 시(v6 벤치): **~328초/장 (~11장/h)** — **14배 변동!**
- → **Tier3 = 공유 GPU**라 이웃 부하에 성능 출렁임(전용 H100과 대조). 깨끗한 값은 **한가한 시간/전용 티어**에서 재측정 필요.
- gcube 5060Ti 시간당 가격 미확보 → 원/장 계산 대기.

**✅ A100-SXM4-40GB (gcube) — 측정 완료 (느리고 비쌈!)**
- 성능: batch1 **4.08s·882장/h** / batch8 **3.11s·1,157장/h**(VRAM22GB) / batch16 OOM(40GB). **5090보다 느림**(신형 5090 > 구형 A100, 이 작은 모델에선).
- 가격: 실청구(14:40, **util 98% = 깨끗**) 1,044.38원 → **all-in ~4,273원/hr** (기본금 **2,991원/hr**[5090의 3배!] + 이용 1,282). → 원/장 **~3.69원**(batch8). **셋 중 최악.**
- 함의: gcube A100은 이 워크로드에 부적합(느림+기본금 큼). **"A100+5060 혼합" 아이디어 약화** — 5090 위주가 나음.

**※ 환율 정정: 1$ = 1,525원** (앞서 1,350 가정 정정). H100 = $3.30 → **5,033원/hr** → 원/장 batch8 **1.94원**. **원/장 정리(batch8): gcube 5090 ~1.24(추정) < RunPod H100 1.94(실측) < gcube A100 3.69(실측).**

**❌ RTX 5060 (plain, 8GB) — 이 모델 부적합 (제외)**
- VRAM 모드로 측정 시 384초/장(9.4장/h). 원인: **uint4 모델 peak 8.46GB > 카드 7.96GB → 넘쳐서 시스템 RAM 폴백(spill) → 초저속.**
- 8GB는 MEM_MODE=RAM(offload)이어야 맞지만 느림 → **이 워크로드엔 부적합. 16GB 이상 필요.** (제품 관점: 최저가 8GB 카드로는 이 모델 못 굴림)

**핵심 발견**
- **배치가 uint4에서 정상 작동**(H100 batch32까지 OOM 없음) — B-1 핵심 질문 해결.
- 배치 이득 **~1.55배, batch 8 sweet spot** 이후 포화(연산 한계).
- **둘 다 compute-bound**: batch1부터 util 98%, VRAM 남아도 throughput 안 늚 → "VRAM 채우기(배치)"보다 **동시성(여러 대)이 핵심 전략**일 가능성.
- **gcube Tier3 5060 성능 14배 변동(공유) vs RunPod H100 일정(전용)** — 비교/제품 스토리에 큰 함의.

**측정 도구 주의:** benchmark는 **command 오버라이드 일회성**으로(서버랑 같이 실행 금지 — 16GB에 모델 2개 로드 시 OOM/초저속, 실제로 143초 겪음). 16GB 노드는 서버 없이 벤치 단독으로만.

---

## 6. 인수인계 메모
- 사람이 읽는 셋업·작업방식 가이드: **`인수인계_가이드.md`**.
- 이 프로젝트의 작업은 "기록을 남기고 → 새 대화에서 불러오고 → 메모리에 저장"하는 방식으로 이어진다. 이 문서를 항상 최신으로 유지하는 것이 그 핵심이다.
