# GPU 측정 기록 — v10 (동일조건 재측정)

**작성일:** 2026-06-12 · **이미지:** `yjoh/z-image-light:v10` · **모델:** Z-Image-Turbo SDNQ uint4

> 같은 워크로드(1024 이미지 생성)를 GPU만 바꿔 **완전히 동일한 조건**으로 측정해, gcube 카드들 vs 타사(RunPod H100)의 속도·비용을 공정 비교한 기록.

---

## 1. 왜 v8 → v10으로 업데이트했나 (이게 핵심)

이전 측정(v6~v8)에는 **공정 비교를 망치는 문제 3가지**가 있었다. v10에서 전부 수정.

### ① Triton 비활성 (eager 폴백) — 모든 카드 속도 과소측정
- 증상: 로그에 `SDNQ: Triton test failed! Falling back to PyTorch Eager mode. Failed to find C compiler.`
- 원인: 이미지에 **C 컴파일러(gcc)가 없어서** SDNQ uint4 최적 커널(inductor 컴파일 경로)이 작동 못 함 → 느린 eager 모드로 폴백.
- 영향: **H100·5090·A100 등 모든 속도 측정이 eager = 실제보다 느리게 측정됨.** (예: H100 2.07s→실제 1.67s, ~19% 차이)
- **수정(v10):** Dockerfile에 `build-essential` + `python3-dev` 추가 → Triton/inductor 작동.

### ② benchmark.py 조건 불일치 — 카드 간 비교 불공정
- 증상: `benchmark.py`가 VRAM 모드에서 **VAE 타일링을 안 켜서**, 실제 서비스(`server.py`, 타일링 항상 ON)와 조건이 달랐음. 게다가 8GB(5060)는 타일링 없으면 스필.
- **수정(v10):** `benchmark.py` 로드 로직을 `server.py`와 **완전히 동일하게**(타일링 항상 ON) → 모든 카드 동일조건 + 5060도 benchmark로 측정 가능.

### ③ 빌드 재현성 — torch 버전 미고정
- 증상: `torch torchvision`(버전 미지정) → 빌드마다 최신 torch를 받아, CI에서 torchvision 매칭 실패 + 테스트 이미지와 버전 달라질 위험.
- **수정(v10):** `torch==2.11.0 torchvision==0.26.0` (+cu128) 고정 → 재현성 + 테스트 이미지와 동일 보장.

### (부수) 리팩토링
- `server.py`: MEM_MODE 기본값을 VRAM으로(8GB도 타일링으로 1024 가능), gc.collect는 RAM 모드에서만.
- `static/app.js`/`server.py`: 안 쓰는 `/api/resources` 폴링·죽은 코드 제거(요청 4→3).

**결론:** v10 = "**같은 조건 + Triton ON + 타일링 ON**"으로 공정 측정이 가능한 이미지.

---

## 2. 측정 조건 (모든 카드 고정 — 이게 "공정"의 핵심)

| 항목 | 값 |
|---|---|
| 모델 | Disty0/Z-Image-Turbo-SDNQ-uint4-svd-r32 |
| 해상도 | 1024×1024 |
| step | 8 |
| guidance | 0.0 |
| MEM_MODE | VRAM (타일링 항상 ON) |
| Triton | ON (gcc) |
| **batch** | **1** ← 5060 8GB가 batch1만 가능 → **공통분모로 통일**(공정 비교) |
| 이미지 | yjoh/z-image-light:v10 |

> ⚠️ batch1로 통일한 이유: 5060(8GB)은 배치를 못 한다. H100/5090 등 큰 카드는 batch8에서 더 빠르지만(자기 강점), "같은 작업"으로 공정 비교하려면 **모든 카드가 할 수 있는 batch1**이 기준. 큰 카드의 batch8 이점은 별도(각주)로 본다.

---

## 3. 측정 방법 (순서대로)

### A. 속도(throughput) 측정 — 모든 카드
1. 해당 카드 노드에 **v10 이미지로 워크로드 배포** (서버 띄우지 않고 benchmark 단독)
2. 컨테이너 **command 오버라이드**:
   ```sh
   sh -c "python3 /app/benchmark.py --mem-mode VRAM --batch 1 --runs 40 --warmup 2 --note '카드명' 2>&1 | tee /workspace/bench_카드.txt; sleep infinity"
   ```
   - `--warmup 2`: 첫 1~2장(Triton 컴파일 워밍업) 버림
   - `--runs 40`: 40장 평균
   - `sleep infinity`: 끝나도 파드 살려둬 로그 읽기 (읽고 **수동 중단**)
3. 로그의 `===== 요약 =====`에서 **장당 시간 / 장/h** 확인 → 워크로드 중단
4. (참고) 첫 장의 `torch/_inductor ... Extern Choice` 경고는 **컴파일 워밍업 노이즈, 무해**

### B. 가격(원/장) 측정 — gcube 카드만
1. A와 같되 **길게**: `--runs 320` (생성이 청구 시간의 대부분을 차지하게 → 오버헤드 희석)
2. 요약 뜨면 **즉시 워크로드 중단** ← `sleep infinity` 대기시간(idle)이 청구에 붙는 것 방지 (**안 지키면 원/장 오염**)
3. gcube **청구 상세**에서 그 파드의 **금액 + 이용시간 + GPU 이용율** 확인
4. **원/장 = (총 금액 ÷ 이용시간[hr]) ÷ throughput[장/h]**
   - util이 99% 근처면(풀가동) 이 값 = 청구÷생성장수와 일치 → 깨끗
   - 예) 5090: 444.23원 ÷ 0.1997hr = 2,224원/hr ÷ 1,210장/h = **1.84원/장**

### C. H100 (RunPod)
- 같은 benchmark 명령(`tee`는 /workspace 없으면 생략)
- 가격은 **RunPod 요금표**($3.30/hr 등) — 측정 불필요

---

## 4. 결과 (batch1, v10, 동일조건)

| 카드 | 공급사 | 장당 | 장/h | 원/장 | 비고 |
|---|---|---|---|---|---|
| **RTX 5090** | gcube | 2.97s | 1,210 | **1.84원** | 🥇 전체 최저원가·안정(Tier2) |
| A100-80GB | 타사 RunPod | 3.52s | 1,024 | 2.18원 | $1.49/hr Secure |
| **H100 80GB** | 타사 RunPod | 1.67s | 2,158 | **2.29원** | $3.30/hr Secure·최고속 |
| RTX 4090 | gcube | 4.56s | 789 | ~3.7원(잠정) | throttle 의심·재측정 권장 |
| **H100 80GB** | gcube | 1.68s | 2,142 | **~4.2원** | 기본금 6,440/hr |
| A100-40GB | gcube | 3.87s | 930 | ~4.6원 | 대형·고메모리 작업용 |
| RTX 5060 8GB | gcube | ~24s(비혼잡) | ~150 | 가격대기 | Tier3 공유=변동(혼잡 49~74s) |

> 환율 1$=1,500원. 타사 가격 둘 다 RunPod **Secure** 티어(일관). 모두 batch1 동일조건.

### 핵심 비교 — 두 결론

**① 이 워크로드 최저원가 = gcube 5090 (1.84원)**
- 타사 H100(2.29)·A100-80GB(2.18)보다도 **쌈**. → 가벼운 이미지 생성엔 **비싼 데이터센터 카드 불필요, gcube 5090이 최저원가.**
- batch1 동일조건. (각자 최적배치면 5090 batch8 1.63 vs H100 batch8 1.94 = ~16%)

**② 같은 칩이면 타사가 쌈 = gcube 데이터센터 카드는 기본금 탓에 비쌈**
- H100: 타사 2.29 < gcube 4.2 (~1.9배). 속도는 동일(1.67/1.68s, 같은 칩) → 차이는 순수 가격(기본금).
- A100: 타사 2.18 < gcube 4.6 (단 변형 다름: 타사 80GB / gcube 40GB).

**→ 정직한 메시지:** "gcube가 다 싸다"는 거짓. **"이 작업엔 gcube 5090이 최저원가(타사 H100보다 ~20% 싸고, 응답속도도 충분)"가 진실.** 데이터센터급(H100/A100)은 타사가 낫고, **gcube의 강점은 소비자 카드(5090).**

**기타:**
- Triton 효과: 강카드만(H100 ~19%·5090 ~5%·5060 ~0). 약한 카드는 raw 연산 병목.
- A100 < 5090(이 워크로드): 신형 소비자 GPU > 구형 데이터센터(작은 uint4 모델). A100은 "용도가 다른"(대형·고메모리) 카드 — **"최악" 표현 금지, 홍보엔 "작업맞춤 선택지".**

---

## 5. 주의사항 (gotchas)
- **즉시 중단:** benchmark 끝나면 바로 워크로드 중단(`sleep infinity` idle 과금 방지). 가격 측정에 특히 치명적.
- **Tier3 공유(5060) = 변동 큼:** 깨끗한 값은 한가한 시간(오프피크)에. util 99%인데 느리면 = 쪼개 쓰는 중.
- **batch1 통일:** 5060 공통분모. 큰 카드 batch8 이점은 각주.
- **옛 v6/eager 측정값은 폐기:** Triton 없던 값이라 신뢰 X. v10 값만 사용.
- **첫 장 inductor 경고:** 정상(컴파일 워밍업). 무해.
- **4090 throttle 의심:** A100보다 느린 건 비정상 → gcube 4090 전력제한/공유 의심, 다른 티어 재측정 권장.

---

## 6. 진행 상태 / 남은 일
- [x] **활용 시나리오 탭 v10 실측 교체 완료**(2026-06-12): 동시/대량/구성 하드코딩 → 5090 1.84·H100 2.29·A100 4.59, % 재계산(동시·대량 ~20%). 파일: index.html·static/app.js·_preview_usecase.html.
- [x] 비교 기준 = **batch1 주력 + batch8 각주**로 확정.
- [ ] **5060 8GB 가격** (오프피크 비혼잡 클린 측정 → 원/장). 탭 5060Ti 슬롯도 그때 채움.
- [ ] **4090 재측정**(다른 티어 — throttle 확정).
- [ ] (선택) gcube H100 정밀 청구(현재 rate 기반 ~4.2원).
